# Phase 11A LAN-only LiveKit development configuration.
# Regenerate this file when the PC's LAN address changes.

port: 7880

bind_addresses:
  - "0.0.0.0"

rtc:
  node_ip: "10.136.70.214"
  use_external_ip: false
  tcp_port: 7881
  udp_port: 7882

keys:
  devkey: secret

logging:
  level: debug
